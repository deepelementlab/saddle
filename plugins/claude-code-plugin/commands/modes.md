---
description: List Saddle modes via saddle serve API
---

Run the bundled script (prints JSON):

```bash
node "${CLAUDE_PLUGIN_ROOT}/commands/scripts/list-modes.js"
```

If it fails, ensure **`saddle serve`** is running and `SADDLE_BASE_URL` points to it.

Summarize the `modes` array and `path` for the user.
